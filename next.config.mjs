/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Replace with real optimized domains / loader when photography is supplied.
    unoptimized: true,
  },
};

export default nextConfig;
